sudo: false
language: node_js
matrix:
  include:
    - os: linux
      node_js: '10'
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version v1.9.4
  - export PATH="$HOME/.yarn/bin:$PATH"
script:
  - 'yarn run build-prod'
